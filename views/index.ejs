<h2>Recently Added</h2>

<% books.forEach((book, id) => { %>
<span>
    <% (id + 1) %> 
  <img height="150" width="100" src="<%= book._cover %>" />
  <p>Title: <%= book.title %></p>
  <p>Description: <%= book.description %></p>
  <p>Author: <%= book.author %></p>
</span>
<% }) %>
