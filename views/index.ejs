<h2>Recently Added</h2>

<% books.forEach((book, id) => { %>
<span>
    <% (id + 1) %> 
  <img height="150" width="100" src="<%= book.cover_src%>" />
  <p>Title: <%= book.title %></p>
  <p>Description: <%= book.description %></p>
  <p>Author: <%= bookAuthors[id].name %></p>
</span>
<% }) %>
